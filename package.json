{
  "name": "x-log",
  "version": "0.3.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv --override=false -- turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,js,jsx}\"",
    "type-check": "turbo run type-check",
    "migrate": "dotenv --override=false -- turbo run migrate",
    "start": "dotenv --override=false -- turbo run start"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.1.0",
    "shadcn": "^3.8.5",
    "turbo": "^2.6.3",
    "typescript": "^5.3.3"
  },
  "packageManager": "bun@1.0.0",
  "engines": {
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.2.4",
    "dotenv": "^17.2.3"
  },
  "overrides": {
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3"
  }
}
